import arbitrage from './arbitrage';

describe('arbitrage testing suite', () => {
    test('sample test case', async() => {
        //const response = await arbitrage();
        //console.log(response);
        expect(true).toEqual(true);
    })
})